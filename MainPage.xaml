<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PIM_VIII.MainPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Gray900}">

    <ScrollView>
        <VerticalStackLayout
            Padding="20,10"
            Spacing="25">

            <Entry
                Text="Pesquisar"
                TextColor="{StaticResource Gray500}"
                BackgroundColor="{StaticResource Gray200}"
                MaximumWidthRequest="300">
                
            </Entry>

            <HorizontalStackLayout>
                <VerticalStackLayout>
                    <Border StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10" />
                        </Border.StrokeShape>
                        <Image
                            Source="Youtube_Thumbnail.png"
                            HeightRequest="185"
                            Aspect="AspectFill"
                            SemanticProperties.Description="dot net bot in a race car number eight" 
                            />
                    </Border>
                        <Label
                        Text="Forest fires, natural or human factors?"
                        FontAttributes="Bold"
                        SemanticProperties.HeadingLevel="Level1" 
                        TextColor="White"/>
                </VerticalStackLayout>
            </HorizontalStackLayout>

            <TableView Intent="Menu">
                <TableRoot>
                    <TableSection Title="Seus Vídeos">
                        <ImageCell Text="1. Introduction"
                       Detail="Learn about your XBox and its capabilities."
                       ImageSource="youtube_thumbnail_mini.png" />
                        <ImageCell Text="2. Turn it on"
                       Detail="Learn how to turn on your XBox."
                       ImageSource="xbox.png" />
                        <ImageCell Text="3. Connect your controller"
                       Detail="Learn how to connect your wireless controller."
                       ImageSource="xbox.png" />
                        <ImageCell Text="4. Launch a game"
                       Detail="Learn how to launch a game."
                       ImageSource="xbox.png" />
                    </TableSection>
                </TableRoot>
            </TableView>

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
